# 诺艾尔AI系统 技术白皮书

## Noelle AI System — 高拟人化二次元角色AI交互系统

**版本：** v3.2  
**最后更新：** 2026年6月  
**许可证：** LGPL-2.1  

---

## 摘要

诺艾尔AI系统（Noelle AI System）是一个基于Node.js的模块化高拟人化AI交互系统，内置固定二次元角色「诺艾儿」——一位内向、胆小、依赖用户的猫耳少女。系统集成了流式LLM推理、OpenRouter深度集成（动态模型列表、提供商路由、推理参数、插件系统）、13阶段JSON自动修复引擎、多模态视觉理解、MCP (Model Context Protocol) 工具协议、WebSocket实时服务、Web管理界面、OpenAI兼容API、多对话管理、自动记忆进化与深度复盘机制。本白皮书从技术架构、核心原理、通信协议、安全模型及生态对比等维度进行全面阐述。

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [核心流式引擎](#3-核心流式引擎)
4. [多后端抽象层](#4-多后端抽象层)
5. [思考标签解析系统](#5-思考标签解析系统)
6. [MCP工具系统](#6-mcp工具系统)
7. [多模态视觉处理](#7-多模态视觉处理)
8. [记忆与进化系统](#8-记忆与进化系统)
9. [多对话系统](#9-多对话系统)
10. [Web管理界面](#10-web管理界面)
11. [通信协议层](#11-通信协议层)
12. [人物数值系统](#12-人物数值系统)
13. [配置与部署](#13-配置与部署)
14. [安全模型分析](#14-安全模型分析)
15. [在线更新系统](#15-在线更新系统)
15. [在线更新系统](#15-在线更新系统)
16. [生态对比](#16-生态对比)
17. [总结与展望](#17-总结与展望)

---

## 1. 项目概述

### 1.1 定位

诺艾尔AI系统是一套**面向个人/本地使用的拟人化AI伴侣应用**，与通用AI框架（如LangChain、Semantic Kernel）不同，它专注于提供一个完整、可开箱即用的角色扮演体验，而非开发工具链。

### 1.2 核心特性矩阵

| 维度 | 特性 |
|------|------|
| **角色** | 固定人设（诺艾儿），游戏化数值（HP/MP/好感度/依赖度） |
| **推理** | 流式生成，`<thinking>` 深度思考解析，5种推理策略 |
| **多模态** | 图像分析、剪贴板粘贴、文件附加 |
| **工具** | MCP标准13大工具（读/写文件、Web搜索/抓取、Shell、图像分析、对话管理×7） |
| **协议** | WebSocket实时推送、OpenAI兼容API、MCP JSON-RPC |
| **记忆** | 自动规则提取、事实记录、深夜复盘日记、思考日志 |
| **意识** | 闲时主动思考，WebSocket推送主动对话 |
| **前端** | Web管理界面（状态面板/对话列表/内心独白弹窗） |
| **对话** | 多对话隔离存储，新建/切换/搜索/删除 |

### 1.3 技术栈

| 组件 | 技术 |
|------|------|
| 运行时 | Node.js >= 16.x |
| HTTP/HTTPS | Express 4.x + cors |
| WebSocket | `ws` 库 |
| MCP协议 | `@modelcontextprotocol/sdk` ^1.29.0 |
| 终端UI | `chalk` / `readline-sync` / `inquirer` |
| 图像处理 | `sharp`（JPEG压缩/缩放） |
| 剪贴板 | 原生实现（PowerShell/pbpaste/xclip） |
| Web前端 | 原生HTML/CSS/JS（无框架） |
| 代码体积 | ~2782行（src/ 20个模块）+ 1034行（web/ 3个文件） |

---

## 2. 系统架构

### 2.1 架构总览

```
┌────────────────────────────────────────────────────────────────────┐
│                        诺艾尔 AI 系统 v3.2                          │
│                                                                    │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐  ┌──────────────────┐ │
│  │ CLI 终端  │  │ Web 界面  │  │ WebSocket │  │   OpenAI兼容API  │ │
│  │ 沉浸式聊天│  │状态面板   │  │ 实时服务   │  │  /v1/chat/       │ │
│  │ 命令系统  │  │对话列表  │  │ 双向推送   │  │  completions     │ │
│  └────┬─────┘  └────┬─────┘  └─────┬─────┘  └────────┬─────────┘ │
│       │              │              │                  │           │
│  ┌────┴──────────────┴──────────────┴──────────────────┴────────┐ │
│  │                    核心对话引擎 (core/chat.js)                 │ │
│  │   chatWithOwen — JSON修复 — 工具调用递归 — 思考日志持久化      │ │
│  └───────────────────────────┬──────────────────────────────────┘ │
│                              │                                     │
│  ┌───────────────────────────┴──────────────────────────────────┐ │
│  │                  提示词构建 (core/prompt.js)                   │ │
│  │   buildMultiRoundMessages — AI.PROMPT模板 — 5种推理策略       │ │
│  └───────────────────────────┬──────────────────────────────────┘ │
│                              │                                     │
│  ┌───────────────────────────┴──────────────────────────────────┐ │
│  │                 流式请求引擎 (llm/stream.js)                   │ │
│  │  streamAttemptRequest — 3后端统一抽象 — <thinking>标签解析器   │ │
│  └───────────────────────────┬──────────────────────────────────┘ │
│                              │                                     │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐  ┌──────────────────┐ │
│  │ MCP服务   │  │ 多模态    │  │ 记忆进化   │  │ 潜意识沉思      │ │
│  │13工具     │  │ 图像处理  │  │深度复盘   │  │主动思考循环    │ │
│  │资源/提示  │  │ 剪贴板   │  │思考日志   │  │WebSocket推送   │ │
│  └──────────┘  └──────────┘  └───────────┘  └──────────────────┘ │
│                                                                    │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐                       │
│  │ 多对话管理 │  │ 配置文件  │  │ Web前端    │                       │
│  │ 隔离存储   │  │ .noll-env│  │ index.html│                       │
│  │ JSON持久化 │  │ 30项配置  │  │ app.js    │                       │
│  └──────────┘  └──────────┘  └───────────┘                       │
└────────────────────────────────────────────────────────────────────┘
```

### 2.2 模块化架构设计

系统从v3.0的单文件单体架构（2211行）重构为**多模块架构**（20个源文件，~2782行），新增**Web前端**（3个文件，1034行）：

| 模块 | 文件 | 行数 | 职责 |
|------|------|------|------|
| 入口编排 | `src/index.js` | 138 | 服务启动、端口管理、命令解析 |
| 配置 | `src/config.js` | 99 | .noll-env 加载/保存，44项配置 |
| 状态 | `src/state.js` | 74 | HP/MP/好感度/依赖度，记忆环形队列 |
| 聊天引擎 | `src/core/chat.js` | 178 | chatWithOwen 主循环，13阶段JSON自动修复 |
| 提示词 | `src/core/prompt.js` | 122 | AI.PROMPT模板 + 5策略 + 上下文构建 |
| 意识 | `src/core/consciousness.js` | 87 | 闲时主动思考，WebSocket推送 |
| 进化 | `src/core/evolve.js` | 106 | 记忆进化、深度复盘、思考日志 |
| LLM流式 | `src/llm/stream.js` | 373 | 3后端统一抽象，OpenRouter深度集成，reasoning_details，流式usage |
| MCP服务 | `src/mcp/server.js` | 449 | 13工具 + 资源 + 提示词 + 动态注册 |
| API服务 | `src/api/server.js` | 159 | OpenAI兼容API + 静态文件服务 |
| WS服务 | `src/ws/server.js` | 67 | WebSocket双向通信 |
| 对话管理 | `src/ws/conversations.js` | 94 | 多对话JSON持久化 |
| CLI聊天 | `src/cli/chat.js` | ~440 | 命令系统、模型详情、文件选择、剪贴板 |
| 向导 | `src/cli/wizard.js` | 119 | 首次运行配置向导，动态模型列表，推理/路由配置 |
| 文件选择 | `src/cli/picker.js` | 92 | 多级目录导航选择 |
| 终端UI | `src/ui/display.js` | 102 | 打字机效果、思考流、旋转动画 |
| 模型查询 | `src/utils/models.js` | 105 | OpenRouter动态模型列表获取/缓存/筛选 |
| 剪贴板 | `src/utils/clipboard.js` | 61 | 跨平台图像获取 |
| 图像 | `src/utils/image.js` | 52 | sharp处理管线，动态视觉检测 |
| 端口 | `src/utils/port.js` | 48 | 端口检测/释放 |
| **Web 前端** | | | |
| HTML | `web/index.html` | 111 | 主界面结构 |
| CSS | `web/style.css` | 520 | 暗色调主题样式 |
| JS | `web/app.js` | 403 | WebSocket客户端 + 状态动画 |

### 2.3 数据流

```
用户输入 → 多对话路由(ws/conversations.js) 
  → 构建上下文(buildMultiRoundMessages) 
    → 流式请求(askLLMStream → streamAttemptRequest) 
      → LLM后端(流式返回token)
      → OpenRouter推理token分流(直接→deepThinkContent，绕过<thinking>解析器)
      → <thinking>标签解析器(分流角色内心独白/回复)
      → 流式usage捕获(精确token统计)
    → JSON修复引擎(repairJSON, 13阶段自动修复)
    → 模型纯文本兜底(提取模型自然语言回复，无需重试)
    → 工具调用(若tool_call非空, executeTool + 递归chatWithOwen)
  → 状态更新(updateStatus: Like/Desire)
  → 记忆存储(addMemory → memory[])
  → 思考日志持久化(saveThinkLog → think_logs/)
  → 对话持久化(saveConversation → conversations/{id}.json)
  → 触发进化/复盘(条件判断: 每6/12条消息)
  → 输出回复(CLI打字机 / WebSocket推送 / API响应)
```

---

## 3. 核心流式引擎

### 3.1 streamAttemptRequest 设计

核心流式函数 `streamAttemptRequest(promptConfig, onStructuredChunk)`（`src/llm/stream.js`）是所有LLM交互的枢纽。其设计要点：

```
返回: Promise<{ content: string, think: string }>
参数: 
  - promptConfig: { prompt, n_predict, temperature, top_p, repeat_penalty, stop, images }
  - onStructuredChunk: 可选的实时回调，接收 { type: 'think'|'content', text }
```

### 3.2 流式状态机

```
┌─────────┐     开始请求     ┌───────────┐
│  初始态   │ ─────────────→ │ 等待首token │
└─────────┘                └───────────┘
                                │
                         收到数据块(chunk)
                                │
                           ┌────┴────┐
                           │ 标签解析  │ ← ← feed到parseAndEmit
                           └────┬────┘
                           ┌────┴────┐
                     ┌─────┤ 判断状态  ├─────┐
                     │     └─────────┘     │
                     ▼                     ▼
               ┌──────────┐         ┌──────────┐
               │ 思考中    │         │ 回复中    │
               │ (thinking)│         │ (content) │
               └──────────┘         └──────────┘
                     │                     │
              思考结束标签              流结束
                     │                     │
                     ▼                     ▼
               ┌──────────┐         ┌──────────┐
               │ 切换到回复  │         │ resolve   │
               └──────────┘         └──────────┘
```

### 3.3 关键设计决策

- **带超时的Promise包装**：`withTimeout()` 使用 `Promise.race` 实现，默认300秒超时
- **防重复resolved**：`finishOnce` 使用闭包变量 `resolved` 确保只resolve一次
- **流式usage捕获**：从OpenRouter最终chunk捕获精确 `usage` 数据（prompt_tokens / completion_tokens）
- **OpenRouter推理token分流**：`delta.reasoning` / `delta.reasoning_content` / `delta.thinking` / `reasoning_details[]` 直接累加到 `deepThinkContent` 并以 `'think'` 类型发出，绕过 `<thinking>` XML解析器，避免推理文本混入角色回复
- **JSON修复引擎**（`src/core/chat.js`）：13阶段自动修复管线——提取代码块、移除注释、修复引号/逗号/括号/转义，自动补全缺失 `}`，提取首个完整JSON对象
- **零重试策略**：模型未输出JSON格式时不再重复请求（节省时间与token），直接通过 `extractPlainText` 提取纯文本作为回复

---

## 4. 多后端抽象层

### 4.1 后端类型枚举

| 类型 | 使用场景 | 流式格式 | 思考标签支持 |
|------|----------|----------|-------------|
| `llama.cpp` | 本地CPU/GPU推理 | `/completion` 逐行JSON | 无原生，由客户端标签包裹 |
| `ollama` | 本地Ollama服务 | `/api/chat` SSE | 原生 `message.thinking` 字段 |
| `api` | 远程API调用 | SSE流式 | 多厂商兼容字段 |

### 4.2 多厂商兼容设计

系统的在线API模块（`src/llm/stream.js`）对多个AI厂商进行了适配：

```javascript
// 深度思考字段兼容（v3.2新增 reasoning_details 数组支持）
const thinkChunk = data.choices?.[0]?.thinking                    // 通用
  || data.choices?.[0]?.delta?.reasoning_content                  // DeepSeek
  || data.choices?.[0]?.delta?.thinking                           // 其他
  || data.choices?.[0]?.delta?.reasoning;                         // OpenRouter统一字段

const reasoningDetails = data.choices?.[0]?.delta?.reasoning_details;  // OpenRouter结构化推理
```

| 厂商 | 特殊处理 |
|------|----------|
| OpenRouter | `HTTP-Referer` + `X-OpenRouter-Title` 头；`reasoning`/`provider`/`plugins`/`models` 请求体参数 |
| 通用API | `Authorization: Bearer` 标准认证 |

### 4.3 OpenRouter深度集成（v3.2新增）

v3.2对OpenRouter进行了全方位的深度集成：

#### 4.3.1 动态模型列表

新增 `src/utils/models.js` 模块，通过 `GET https://openrouter.ai/api/v1/models` 获取实时模型列表：

```javascript
getModelList()                    // 获取全部模型（5分钟缓存）
findModel(id)                     // 按ID查找模型
supportsVision(id)                // 检测视觉支持
supportsReasoning(id)             // 检测推理支持
supportsTools(id)                 // 检测工具调用支持
getModelContextLength(id)         // 获取上下文长度
getModelPricing(id)               // 获取定价信息
listModelsByCapability(opts)      // 按能力筛选（免费/视觉/推理/价格）
```

#### 4.3.2 高级采样参数

| 参数 | 类型 | 范围 | 说明 |
|------|------|------|------|
| `min_p` | float | [0, 1] | 相对最小概率阈值 |
| `top_a` | float | [0, 1] | 动态Top-P筛选 |
| `seed` | integer | — | 确定性采样种子 |
| `frequency_penalty` | float | [-2, 2] | 词频惩罚 |
| `presence_penalty` | float | [-2, 2] | 主题多样性惩罚 |
| `repetition_penalty` | float | (0, 2] | 重复惩罚系数 |

#### 4.3.3 推理参数（reasoning）

```javascript
reqBody.reasoning = {
  effort: 'high',         // xhigh/high/medium/low/minimal/none
  max_tokens: 2000,       // 推理token上限
  exclude: false           // 是否在响应中排除推理内容
}
```

支持自动检测：当通过 `supportsReasoning()` 检测到模型支持推理且用户未手动设置时，自动启用 `medium` 力度。

#### 4.3.4 提供商路由（provider）

```javascript
reqBody.provider = {
  sort: 'price',                          // price/throughput/latency
  order: ['anthropic', 'openai'],         // 优先提供商顺序
  allow_fallbacks: true,                  // 是否允许回退
  // 还支持: only, ignore, quantizations, max_price,
  //         preferred_min_throughput, preferred_max_latency
}
```

#### 4.3.5 插件系统（plugins）

```javascript
reqBody.plugins = [
  { id: 'file-parser', pdf: { engine: 'mistral-ocr' } },  // PDF解析
  { id: 'response-healing' },                               // JSON自动修复
  { id: 'context-compression' }                             // 上下文压缩
]
```

#### 4.3.6 模型回退（models）

```javascript
reqBody.models = ['openai/gpt-4o', 'anthropic/claude-sonnet-4'];  // 备用模型列表
```

### 4.4 Ollama思考深度支持

系统维护了一个已知支持思考的Ollama模型列表：

```javascript
const ollamaThinkingModels = ['deepseek-r1', 'qwen3', 'deepseek-v', 'qwen2.5-vl', 'glm-4'];
```

当检测到使用这些模型时，自动在请求体中添加 `think: true` 参数，并开启 `keep_alive: "30m"` 保持模型常驻。

---

## 5. 思考标签解析系统

### 5.1 解析器设计

流式响应中实时解析 `<thinking>` XML标签的算法（`src/llm/stream.js`）：

```
parseAndEmit(rawChunk):
  1. 将chunk追加到 contentBuffer
  2. 循环处理 contentBuffer:
     a. 如果 thinkingTagDone == false:
        - 如果 inThinkingTag == false:
          * 查找 <thinking> 或  开始标签
          * 找到: 标签前内容→emit('content'), 进入思考模式
          * 未找到: 保留尾部100字符用于标签检测, 其余→emit('content')
        - 如果 inThinkingTag == true:
          * 查找 </thinking> 或  结束标签
          * 找到: 标签内内容→emit('think'), 标记结束
          * 未找到: 全部内容→emit('think')
     b. 如果 thinkingTagDone == true:
        - 剩余全部内容→emit('content')
```

### 5.2 双标签兼容

系统同时支持两种思考标签格式：

```javascript
const tag1 = contentBuffer.indexOf('<thinking>');
const tag2 = contentBuffer.indexOf('');
```

这种设计兼容了不同模型的输出习惯。

### 5.3 推理token vs 思考标签的双层分离（v3.2）

v3.2 引入推理token与角色思考标签的**双层分离架构**：

```
LLM流式响应
  │
  ├── delta.reasoning / delta.thinking / reasoning_details[]  (OpenRouter推理token)
  │   └── 直接→ deepThinkContent, emit('think')   ← 绕过parseAndEmit
  │
  └── delta.content  (模型输出的文本内容)
      └── parseAndEmit() → 查找<thinking> XML标签
          ├── 标签前内容 → emit('content')         ← 角色回复
          ├── <thinking>...</thinking> → emit('think')  ← 角色内心独白
          └── 标签后内容 → emit('content')         ← 角色回复
```

这种设计确保了：
1. **OpenRouter的原生推理过程**（reasoning tokens）不会被误当作角色回复
2. **角色内心独白**（`<thinking>` XML标签）作为角色人设的一部分正常解析
3. 最终 `{ content, think }` 返回值中，`think` 可能包含两种思考内容的拼接

### 5.4 实时流式反馈

与思考标签解析联动的终端反馈系统（`src/ui/display.js`）：

- `startThinkingStream()` — 输出黄色 `Thinking...` 提示
- `feedThinkingChunk(chunk)` — 实时输出思考内容
- `endThinkingStream()` — 输出总结并返回完整思考文本

可通过配置项 `ENABLE_DEEP_THINK` 控制是否显示实时思考流。

---

## 6. MCP工具系统

### 6.1 协议实现

系统实现了Anthropic定义的**Model Context Protocol (MCP)** JSON-RPC 2.0标准（`src/mcp/server.js`，449行）。使用官方 `@modelcontextprotocol/sdk` v1.29.0，在 `{wsPort+1}/mcp` 端点提供服务。

### 6.2 协议端点

| JSON-RPC方法 | 功能 |
|-------------|------|
| `initialize` | 返回协议版本 `2025-11-25`、能力声明、服务信息 |
| `tools/list` | 返回13个内置工具的完整定义 |
| `tools/call` | 执行指定工具，返回结构化结果 |
| `resources/list` | 列出4个静态资源 + 1个模板资源 |
| `resources/read` | 读取指定资源内容 |
| `prompts/list` | 列出3个预定义提示词 |
| `prompts/get` | 获取指定提示词内容 |
| `ping` | 心跳检测 |
| `setLevel` | 日志级别设置 |

### 6.3 工具清单（13个）

| 工具名称 | 功能描述 | 安全风险 |
|---------|----------|---------|
| `read_file` | 读取任意文件（最长50K字符） | ⚠️ 任意文件读取 |
| `edit_file` | 替换文件中的文本 | ⚠️ 任意文件写入 |
| `web_fetch` | 获取URL内容（15s超时，最长50K字符） | 低 |
| `web_search` | DuckDuckGo HTML搜索结果解析 | 低（解析HTML，非官方API） |
| `shell_exec` | 执行Shell命令（可配置超时） | 🔴 任意命令执行 |
| `analyze_image` | 视觉模型图像分析（自动检测视觉模型） | 低 |
| `list_conversations` | 列出所有对话（支持关键词筛选） | 无 |
| `get_conversation` | 获取对话历史（支持 lastN 限制） | 无 |
| `create_conversation` | 创建新对话 | 无 |
| `switch_conversation` | 切换到指定对话（自动保存当前） | 无 |
| `delete_conversation` | 删除对话及其JSON文件 | ⚠️ 数据删除 |
| `continue_conversation` | 恢复历史对话上下文 | 无 |
| `search_conversations` | 跨所有对话全文搜索 | 无 |

### 6.4 MCP资源

| URI | 描述 |
|-----|------|
| `noelle://status` | 角色数值 + 记忆计数 |
| `noelle://profile` | 角色档案（rules + facts + 人设） |
| `noelle://conversations` | 所有对话列表（JSON数组） |
| `noelle://config` | 系统配置（敏感字段掩码） |
| `noelle://conversations/{id}` | 单个对话完整历史（模板URI） |
| `noelle://conversations/{id}/recent/{n}` | 单个对话最近N条消息（模板URI） |

### 6.5 MCP提示词

| 名称 | 描述 | 参数 |
|------|------|------|
| `character_intro` | 诺艾儿角色介绍 | 无 |
| `chat_start` | 新对话系统提示词 | `userName?` |
| `conversation_summary` | 对话摘要提示词 | `convId?` |

### 6.6 动态工具注册

系统提供动态注册API，支持运行时扩展工具：

```javascript
registerTool({ 
  name: "my_tool", 
  description: "...", 
  inputSchema: {...}, 
  handler: async (args) => {...} 
})
```

### 6.7 工具调用流程

```
模型输出JSON中带 tool_call → 
  chatWithOwen检测到 tc.name 不为空 →
  打印🔧提示并执行 executeMCPTool(tc.name, tc.arguments) →
  将工具结果格式化为文本 →
  追加到对话上下文中 →
  递归调用 chatWithOwen(followUpPrompt) 继续对话
```

### 6.8 工具结果格式

所有工具返回统一的MCP格式：

```javascript
{ content: [{ type: "text", text: "结果内容" }], isError: false }
```

错误时：

```javascript
{ content: [{ type: "text", text: "错误详情" }], isError: true }
```

`analyze_image` 工具特殊返回 `type: "image_url"` 格式，用于视觉模型的直接图像理解。

---

## 7. 多模态视觉处理

### 7.1 图像处理管线

```
图像来源 →
  1. 文件选择器 (jpg/jpeg/png/gif/bmp/webp)
  2. 剪贴板粘贴 (跨平台)
  3. MCP analyze_image 调用
    ↓
  sharp: resize(宽度≤MAX_IMAGE_SIZE=1024) + JPEG(quality=80)
    ↓
  base64编码 → 构造OpenAI多模态格式 → 注入messages数组
```

### 7.2 剪贴板跨平台实现

`getClipboardImage()`（`src/utils/clipboard.js`）针对三个平台采用不同策略：

| 平台 | 实现方式 |
|------|----------|
| **Windows** | PowerShell `System.Windows.Forms.Clipboard` |
| **macOS** | 外部命令 `pngpaste` |
| **Linux** | 外部命令 `xclip` |

相比v3.0，移除了 `clipboardy` npm依赖，改用原生实现，减小依赖体积。

### 7.3 视觉模型自动检测（v3.2增强）

v3.2 引入了**双层视觉检测策略**：

1. **关键词匹配（同步）**：`isVisionModel()` 使用约40个关键词模式匹配（`vl`, `vision`, `gpt-4o`, `gemini`, `claude`, `qwen`, `llama-3.2-vision`, `pixtral`, `phi-4-vision` 等）
2. **API精确检测（异步）**：`isVisionModelAsync()` 通过 `src/utils/models.js` 的 `supportsVision()` 查询 OpenRouter `input_modalities` 字段

```javascript
async function isVisionModelAsync(modelName) {
  if (config.get('BACKEND_TYPE') === 'api') {
    const result = await supportsVision(modelName);  // 查询 input_modalities
    if (result !== null) return result;
  }
  return isVisionModel(modelName);  // 回退到关键词匹配
}
```

当视觉检测通过时，图像附件会自动格式化为标准的多模态消息：

```javascript
lastMessage.content = [
  { type: "text", text: userText },
  { type: "image_url", image_url: { url: `data:image/jpeg;base64,${base64}`, detail: "auto" } }
];
```

---

## 8. 记忆与进化系统

### 8.1 记忆结构

```
memory: Array<{ role: "user"|"ai", text: string, time: number }>
最大长度: 20条
裁剪策略: 超出时 shift() 移除最旧条目
```

### 8.2 上下文构建

`buildMultiRoundMessages()`（`src/core/prompt.js`）构建发送给LLM的messages数组：

```
System Prompt (AI.PROMPT模板 → 填充NAME/PERSONALITY/HP/MP/DESIRE/LIKE/RULES/FACTS/TOOLS/STRATEGY)
  + 最近4轮对话 (每轮截断200字符)
```

AI.PROMPT模板文件位于项目根目录，可直接编辑修改角色人设和输出格式。

### 8.3 推理策略

系统支持5种推理策略（通过 `STRATEGY` 配置项切换）：

| 策略 | 描述 |
|------|------|
| `standard` | 标准模式，无额外推理标签 |
| `cot` | 思维链（Chain of Thought），逐步推理再回答 |
| `cod` | 代码草稿（Code as Draft），用≤5字草稿记录每一步 |
| `tot` | 思维树（Tree of Thought），生成多条推理路径并选择最佳 |
| `self-refine` | 先回答，再自我批评，然后修正 |

### 8.4 记忆进化 (evolveMemory)

触发条件：每 `EVOLUTION_INTERVAL`（默认为6）条消息后2秒

```
1. 取最近4条对话
2. 调用LLM：提取规则与事实，输出JSON {"add_rules": []}
3. 去重后追加到 profile.rules
4. 保存至 profile.json
```

### 8.5 深度复盘 (deepReflection)

触发条件：每 `REFLECTION_INTERVAL`（默认为12）条消息后2秒

```
1. 取最近对话上下文
2. 角色扮演"深夜反思"场景
3. 调用LLM输出 JSON {"diary_entry": "...", "add_rules": [], "remove_rules": []}
4. 日记追加到 diary.txt 文件
5. 规则变更同步到 profile.json
```

### 8.6 思考日志持久化

每次LLM调用后，`<thinking>` 内容自动保存到 `think_logs/` 目录（由 `SAVE_THINK_LOG` 和 `THINK_LOG_PATH` 配置）：

```
think_logs/think_TIMESTAMP.log
  格式: 用户输入 + AI回复 + 思考内容
```

### 8.7 记忆系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                       记忆系统                               │
│                                                             │
│  用户输入                                                   │
│     │                                                       │
│     ▼                                                       │
│  addMemory("user", text)  →  memory[] (环形队列, 最大20)     │
│     │                                                       │
│     ▼                                                       │
│  chatWithOwen() → 构建上下文(buildMultiRoundMessages) ← AI.PROMPT模板
│     │                                      ← profile.facts/rules (各5条)
│     │                                      ← 多对话记忆 (当前conv.memory)
│     ▼                                                       │
│  addMemory("ai", reply)  →  memory[]                        │
│     │                                                       │
│     ├── 每次回复 → saveThinkLog() → think_logs/             │
│     ├── 每6条消息 → evolveMemory() → 提取规则 → profile.json │
│     └── 每12条消息 → deepReflection() → 日记 → diary.txt    │
│                                                             │
│  !记: 命令 → 直接写入 profile.facts → profile.json           │
│  /conv 命令 → 切换会话 → 保存当前/加载目标 → conversations/  │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. 多对话系统

### 9.1 对话隔离存储

每个对话作为独立的JSON文件存储在 `conversations/{id}.json`：

```javascript
{
  "id": "conv_1717200000000",
  "name": "默认对话",
  "memory": [{ "role": "user", "text": "...", "time": 1717200000 }],
  "HP": 100, "MP": 98, "Desire": 3, "Like": 599,
  "createdAt": 1717200000000,
  "updatedAt": 1717200100000
}
```

### 9.2 对话ID格式

自动生成：`conv_{Date.now()}`

### 9.3 MCP对话管理工具

7个专门管理对话的MCP工具：

| 工具 | 功能 |
|------|------|
| `list_conversations` | 列出所有对话，支持关键词筛选 |
| `get_conversation` | 获取指定对话的完整历史 |
| `create_conversation` | 创建新的空对话 |
| `switch_conversation` | 切换到指定对话（自动保存当前对话） |
| `delete_conversation` | 删除对话及其文件 |
| `continue_conversation` | 恢复历史对话上下文 |
| `search_conversations` | 跨所有对话全文搜索 |

### 9.4 CLI对话管理

CLI聊天中通过以下命令管理对话：

| 命令 | 功能 |
|------|------|
| `/conv` | 对话管理菜单（切换/新建/删除） |
| `/new` | 快速新建空白对话 |

---

## 10. Web管理界面

### 10.1 概述

系统内置了完整的Web管理界面（`web/` 目录，3个文件，1034行），通过OpenAI兼容API服务器的Express静态文件服务托管。

### 10.2 界面布局

```
┌──────────────────────────────────────────────────────────────┐
│  ┌─────────── 侧边栏 (280px) ───────────┬─── 主聊天区域 ────┐ │
│  │  🐱 诺艾尔                           │  消息列表          │ │
│  │  内向胆小猫耳少女                     │  (滚动)           │ │
│  │                                      │                    │ │
│  │  ❤️ HP [████████████░░░] 100         │  诺艾尔正在输入...  │ │
│  │  ✨ MP [████████████░░░] 98          │                    │ │
│  │  💕 好感度 [████████░░░░░] 599      │  ┌──────────────┐  │ │
│  │  🫶 依赖度 [███░░░░░░░░░] 3         │  │ 📎 输入框 发送 │  │ │
│  │                                      │  └──────────────┘  │ │
│  │  😶 安静                             │                    │ │
│  │                                      │                    │ │
│  │  💬 对话列表                         │                    │ │
│  │  ┌─────────────────────┐            │                    │ │
│  │  │ 默认对话            │            │                    │ │
│  │  │ conv_1717200000000  │            │                    │ │
│  │  └─────────────────────┘            │                    │ │
│  │                                      │                    │ │
│  │  模型: qwen2.5-vl:7b                │                    │ │
│  │  [📝 新对话] [🤖 MCP]              │                    │ │
│  │  ● 已连接                           │                    │ │
│  └──────────────────────────────────────┴────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 10.3 功能特性

- **WebSocket实时通信**：自动重连、连接状态指示
- **状态面板**：HP/MP/好感度/依赖度的动态条+数值
- **情绪显示**：13种情绪表情映射（害羞/开心/伤心/生气等）
- **对话列表**：列出所有对话，支持点击切换
- **内心独白弹窗**：点击「💭 查看内心独白」按钮查看思考内容
- **MCP信息弹窗**：查看MCP协议版本、端点列表
- **文件附件**：拖拽/选择文件上传（≤5MB，最多5个）
- **自动重连**：断线后每3秒自动重连
- **打字指示器**：猫咪动画显示"正在输入..."
- **响应式布局**：768px以下隐藏侧边栏

### 10.4 技术实现

- 纯原生HTML/CSS/JavaScript，无任何前端框架
- WebSocket客户端管理连接生命周期
- CSS自定义属性实现暗色调主题系统
- 平滑动画：消息入场(0.3s)、状态条过渡(0.6s)、头像呼吸光晕

---

## 11. 通信协议层

### 11.1 四层协议架构

```
┌──────────────────────────────────────────────────────────────┐
│                    诺艾尔通信协议层                            │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   CLI终端     │  │   Web UI     │  │  WebSocket   │       │
│  │  (readline)   │  │  (浏览器)     │  │  (ws库)      │       │
│  │  交互式输入    │  │  状态面板    │  │  实时双向推送  │       │
│  │  命令补全/历史  │  │  对话列表   │  │  状态广播     │       │
│  │  打字机效果    │  │  内心独白   │  │  主动对话推送  │       │
│  │  文件选择器    │  │  文件上传   │  │  潜意识输出   │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
│         │                 │                 │                │
│         └─────────────────┼─────────────────┘                │
│                           │                                  │
│  ┌────────────────────────┴──────────────────────────────┐   │
│  │                  核心对话引擎                           │   │
│  │  chatWithOwen() — executeTool() — saveThinkLog()      │   │
│  └────────────────────────┬──────────────────────────────┘   │
│                           │                                  │
│  ┌────────────────────────┴──────────────────────────────┐   │
│  │  MCP JSON-RPC Server (wsPort+1)                      │   │
│  │  13 Tools + 4 Resources + 3 Prompts + Dynamic Register│   │
│  └───────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  OpenAI 兼容 API (/v1/chat/completions + /health)     │    │
│  │  SSE流 + 扩展字段 (thinking/emotion/action)          │    │
│  │  静态文件服务 (Web UI， 根路径 '/')                     │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

### 11.2 WebSocket消息格式

服务端→客户端：

```json
{
  "text": "回复内容",
  "action": "肢体动作",
  "情绪": "normal",
  "HP": 85,
  "MP": 76,
  "Desire": 15,
  "Like": 620,
  "role": {
    "name": "诺艾儿",
    "role": "内向胆小猫耳少女",
    "gender": "女",
    "personality": "敏感、缺安全感、轻声细语、依赖先生"
  },
  "thinking": "(前2000字符思考内容)"
}
```

### 11.3 OpenAI API扩展字段

标准OpenAI补全响应中嵌入诺艾尔专有字段：

```json
{
  "id": "chatcmpl-...",
  "object": "chat.completion",
  "choices": [{
    "message": {
      "content": "先生，茶泡好了哦",
      "refusal": null,
      "thinking": "他今天看起来有点累...",
      "emotion": "担心",
      "action": "双手捧着茶杯"
    }
  }],
  "usage": { "prompt_tokens": 128, "completion_tokens": 64, "total_tokens": 192 },
  "noelle": {
    "hp": 85,
    "mp": 76,
    "desire": 15,
    "like": 620,
    "role_info": { "name": "诺艾儿", "role": "内向胆小猫耳少女", "gender": "女", "personality": "敏感、缺安全感、轻声细语、依赖先生" }
  }
}
```

### 11.4 API端点

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/v1/models` | 列出模型（返回配置中的模型信息） |
| POST | `/v1/chat/completions` | 聊天补全（支持流式 + 非流式） |
| GET | `/health` | 系统健康检查（状态 + 数值 + token用量） |
| GET | `/*` | 静态文件服务（Web UI） |

### 11.5 潜意识/意识循环

系统实现了一个后台意识循环（`src/core/consciousness.js`），定时检查用户是否长时间未交互：

```javascript
if (silenceDuration > AFK_THRESHOLD(300s) && Math.random() < 0.1) {
  // 10%概率触发一次主动思考
  await processOneThought(ws);
}
```

主动思考流程：
1. 提示LLM生成内心独白（当前时间、沉默时长、上一个想法）
2. 去重检查（若新想法包含上一个想法前5字符则跳过）
3. 解析JSON `{"thought":"...","should_speak":bool,"message":"...","new_rule":"..."}`
4. 若 `should_speak` 为true，通过WebSocket推送消息给客户端

---

## 12. 人物数值系统

### 12.1 数值定义

| 属性 | 范围 | 初始值 | 含义 |
|------|------|--------|------|
| HP | 0-100 | 100 | 生命值/精力 |
| MP | 0-100 | 98 | 魔力值/心情能量 |
| Desire (依赖度) | 0-100 | 3 | 对用户的依赖程度 |
| Like (好感度) | 0-999 | 599 | 对用户的好感 |

### 12.2 数值更新机制

每次对话回复的JSON中包含 `like_change` 和 `desire_change` 字段（范围[-3, 3]整数）：

```javascript
Like += reply.likeChange;
Desire += reply.desireChange;
Like = Math.max(0, Math.min(999, Like));
Desire = Math.max(0, Math.min(100, Desire));
```

### 12.3 数值注入提示词

每次请求时，当前的HP/MP/Desire/Like值会被写入系统提示词（通过AI.PROMPT模板的 `{{HP}}`、`{{MP}}`、`{{DESIRE}}`、`{{LIKE}}` 占位符），使LLM感知到角色的"状态"，从而影响回复的情感和行为选择。

### 12.4 角色档案 (profile.json)

```
profile.json 结构:
{
  "facts": ["先生喜欢喝红茶", "先生养了一只猫"],   // 通过 !记: 命令添加
  "rules": ["说话要轻声细语", "不能主动提要求"]    // 自动进化生成
}
```

每个事实/规则最多保留最近5条，通过系统提示词注入LLM上下文。

---

## 13. 配置与部署

### 13.1 配置系统 (.noll-env)

配置文件采用JSON格式，包含44个配置项，覆盖网络、后端、视觉、策略、日志、OpenRouter等维度。关键配置项：

| 配置项 | 默认值 | 功能 |
|--------|--------|------|
| `BACKEND_TYPE` | `ollama` | 后端类型选择 |
| `API_MODEL` | `qwen2.5-vl:7b` | 模型名称 |
| `STRATEGY` | `standard` | 推理策略（standard/cot/cod/tot/self-refine） |
| `STREAM_TYPING_SPEED` | 60ms | 打字机效果速度 |
| `THOUGHT_COOLDOWN` | 10000ms | 潜意识触发间隔 |
| `EVOLUTION_INTERVAL` | 6 | 记忆进化频率（消息数） |
| `REFLECTION_INTERVAL` | 12 | 深度复盘频率（消息数） |
| `AFK_THRESHOLD` | 300s | 主动思考静默阈值 |
| `ENABLE_DEEP_THINK` | true | 显示实时思考流 |
| `SAVE_THINK_LOG` | true | 保存思考日志到磁盘 |
| `CONVERSATIONS_PATH` | `./conversations/` | 对话存储目录 |
| `OPENAI_API_ENABLED` | false | 启用OpenAI兼容API |
| `OPENAI_API_PORT` | 8080 | API服务端口 |
| `SSL_ENABLED` | false | 启用SSL/TLS |
| `MAX_IMAGE_SIZE` | 1024px | 图像最大宽度 |
| `IMAGE_QUALITY` | 80 | JPEG质量百分比 |
| `REQUEST_TIMEOUT` | 300000ms | LLM请求超时 |
| **v3.2新增** | | |
| `OPENROUTER_REFERER` | `https://github.com/...` | OpenRouter站点URL（排行榜） |
| `OPENROUTER_TITLE` | `Noelle AI Assistant` | OpenRouter站点名称 |
| `REASONING_EFFORT` | `""` | 推理力度（low/medium/high/xhigh） |
| `REASONING_MAX_TOKENS` | `""` | 推理token上限 |
| `PROVIDER_SORT` | `""` | 提供商排序（price/throughput/latency） |
| `PROVIDER_ORDER` | `""` | 优先提供商列表（JSON数组） |
| `PROVIDER_ALLOW_FALLBACKS` | `true` | 是否允许回退提供商 |
| `PLUGINS` | `""` | 插件配置（JSON数组） |
| `OPENROUTER_MODELS` | `""` | 模型回退列表（JSON数组） |
| `OPENROUTER_USER` | `""` | 最终用户标识 |
| `MIN_P` / `TOP_A` / `SEED` | `""` | 高级采样参数 |
| `FREQUENCY_PENALTY` / `PRESENCE_PENALTY` | `""` | 频率/存在惩罚 |
| `UPDATE_URL` | `""` | 更新服务器地址，留空禁用在线更新 |
| `AUTO_UPDATE` | `true` | 启动时自动检查更新 |

### 13.2 初始化向导（v3.2增强）

首次运行自动进入交互式配置向导（`src/cli/wizard.js`），流程：

```
1. 选择后端类型 (llama.cpp / ollama / 在线API)
   ├── llama.cpp: 检测本地llama-server，确认端口
   ├── ollama: 运行ollama list，列出并选择本地模型
   └── 在线API: 选择厂商 (OpenRouter/硅基流动/DeepSeek/百度/自定义)
       └── OpenRouter:
           ├── 选择模型方式: 在线获取列表 / 手动输入
           │   └── 在线获取: 从 openrouter.ai/api/v1/models 拉取实时列表
           │       └── 筛选: 全部 / 仅免费 / 仅视觉 / 仅推理 / 按价格
           │       └── 排序: 按价格升序，展示ID+定价+上下文+能力标签
           ├── 推理设置: 自动检测 / 启用(选effort) / 禁用
           └── 提供商排序: 默认 / 价格 / 吞吐量 / 延迟
2. 配置 WebSocket 端口 (默认3232)
3. 配置绑定IP (默认127.0.0.1)
4. 选择是否启用OpenAI兼容API
5. 保存到 .noll-env
```

### 13.3 CLI命令系统

CLI聊天中支持的15个命令：

| 命令 | 功能 |
|------|------|
| `/help` | 显示帮助信息 |
| `/quit` 或 `/exit` | 退出程序 |
| `/exit-chat` | 返回主菜单 |
| `/conv` | 对话管理（切换/新建/删除） |
| `/new` | 快速新建空白对话 |
| `/models` | 查看模型信息（Ollama本地列表 / OpenRouter模型详情含上下文/定价/能力） |
| `/update` | 检查并在线更新系统版本 |
| `/url` | 显示所有服务URL（web/ws/mcp/api） |
| `/status` | 显示角色数值 + token使用情况 |
| `/tokens` | 查看当前会话token用量 |
| `/think` | 切换深度思考显示 |
| `@` | 文件选择器（多级目录导航） |
| `!paste` | 从剪贴板读取图像 |
| `!记:xxx` | 记录事实到 profile.facts |
| `!summarize` | 管道模式：文本摘要 |
| `!extract` | 管道模式：提取要点 |
| *(普通文本)* | 正常聊天 |

### 13.4 启动参数

| 参数 | 功能 |
|------|------|
| `--chat` | 跳过主菜单，直接进入CLI聊天 |
| `--update` | 启动后直接进入更新检查界面 |
| `--help` | 显示帮助信息 |

---

## 14. 安全模型分析

### 14.1 安全风险矩阵

| 风险类型 | 严重程度 | 说明 |
|---------|---------|------|
| 任意命令执行 | 🔴 严重 | MCP `shell_exec` 无任何命令白名单 |
| 任意文件读取 | 🟠 高 | MCP `read_file` 可读取任意路径 |
| 任意文件写入 | 🟠 高 | MCP `edit_file` 修改任意文件 |
| 无认证机制 | 🟡 中 | 所有端点（WebSocket/API/MCP）无鉴权 |
| CORS全开放 | 🟡 中 | `Access-Control-Allow-Origin: *` 所有端点 |
| 端口抢占 | 🟡 中 | `killPortProcess` 强制SIGKILL占用进程 |
| 无输入净化 | 🟢 低 | 用户输入直接作为提示词入LLM，依赖LLM安全对齐 |
| 对话数据删除 | 🟡 中 | MCP `delete_conversation` 可删除对话文件 |

### 14.2 设计哲学

系统显然遵循 **本地优先、信任用户** 的安全模型，适用于：

- 个人本地部署（推荐）
- 局域网内家庭使用
- 二次开发测试环境

**不适用于** 面向公网的生产环境部署。

---

## 15. 在线更新系统

### 15.1 设计目标

系统内置完整的在线更新机制，支持从远程更新服务器检查、下载并应用新版本，无需手动下载覆盖。

### 15.2 架构概览（C/S模型）

```
┌─────────────┐     HTTP/JSON     ┌──────────────────┐
│  Noelle客户端  │ ◄─────────────► │  更新服务器        │
│  (src/utils/  │    GET /check    │  (scripts/       │
│   update.js)  │    GET /download  │   update-server.js)│
│              │                  │                  │
│  1. 检查更新   │                  │  版本清单:         │
│  2. 下载包     │                  │  manifest.json    │
│  3. 备份回滚   │                  │  发行包:           │
│  4. 解压覆盖   │                  │  noelle-*.zip     │
│  5. 安装依赖   │                  │                  │
└─────────────┘                  └──────────────────┘
```

### 15.3 客户端接口 (`src/utils/update.js`)

| 函数 | 说明 |
|------|------|
| `checkForUpdate()` | 向 UPDATE_URL/check 发起GET请求，返回最新版本信息或null |
| `downloadUpdate(version, url)` | 下载更新包到临时目录 |
| `applyUpdate(version, zipPath)` | 自动备份当前 src/web/scripts 及配置，解压覆盖，执行 npm install |
| `rollback()` | 回滚到备份版本 |
| `getCurrentVersion()` | 从 package.json 读取当前版本 |
| `compareVersions(a, b)` | 语义化版本比较 (1/0/-1) |
| `getUpdateInfo()` | 返回当前版本、更新地址、自动更新开关 |
| `cleanOldRollbacks()` | 清理超过1个的旧备份 |

**安全机制：**
- 自动排除 `node_modules/`, `conversations/`, `think_logs/` 等数据目录
- 更新前创建 `.rollback/vX.X.X/` 全量备份
- 更新后自动执行 `npm install --production` 安装新依赖

### 15.4 更新服务器 (`scripts/update-server.js`)

轻量HTTP更新服务器，无额外依赖（仅Node.js内置模块）：

| 端点 | 方法 | 说明 |
|------|------|------|
| `/check` | GET | 返回 latestVersion + downloadUrl + changelog |
| `/download?v=X.X.X` | GET | 下载对应版本的 .zip 更新包 |
| `/manifest` | GET | 查看完整发行清单 |

**启动方式：**
```bash
# 默认端口3456，包目录 ./packages/
npm run update-server

# 自定义端口和包目录
node scripts/update-server.js --port=3456 --packages=./packages
```

### 15.5 配置项

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `UPDATE_URL` | `""` | 更新服务器地址，留空禁用在线更新 |
| `AUTO_UPDATE` | `true` | 启动时自动检查更新（仅提示不自动下载） |

### 15.6 CLI命令

- `/update` — 检查更新 → 显示版本对比 → 确认下载 → 备份 → 解压 → 安装依赖
- `--update` 启动参数 — 启动后直接进入更新检查流程
- 启动时自动检测（`AUTO_UPDATE=true`时），发现新版显示提示

## 16. 生态对比

### 16.1 同类项目对比

| 维度 | 诺艾尔 v3.1 | ChatGPT | Ollama WebUI | Character.AI | ShellGPT |
|------|------------|---------|-------------|-------------|---------|
| **角色人设** | 固定二次元角色 | 通用助手 | 无 | 平台化多样角色 | 无 |
| **部署方式** | 纯本地Node.js | 云端SaaS | Docker/Python | 云端SaaS | Python CLI |
| **协议扩展** | MCP + OpenAI API | OpenAI API | OpenAI API | 专有 | 无 |
| **游戏化数值** | ✅ HP/MP/好感度 | ❌ | ❌ | ❌ | ❌ |
| **记忆进化** | ✅ 自动规则提取+复盘 | 有限（Memory功能） | ❌ | ❌ | ❌ |
| **多模态** | ✅ 图像分析 | ✅ DALL·E/视觉 | ✅ 图像 | ❌ | ❌ |
| **工具使用** | ✅ MCP 13工具 | ✅ Function Calling | ❌ | ❌ | ❌ |
| **自主意识** | ✅ 闲时主动思考 | ❌ | ❌ | ❌ | ❌ |
| **Web管理界面** | ✅ 原生HTML/CSS/JS | ✅ 完整Web | ✅ 完整Web | ✅ 完整Web | ❌ |
| **多对话管理** | ✅ 隔离存储+搜索 | ✅ | ✅ | ✅ | ❌ |
| **代码体积** | ~2631行（19模块） | 云端 | 万行级代码 | 云端 | 数千行 |
| **许可证** | LGPL-2.1 | 专有 | MIT | 专有 | MIT |

### 16.2 核心差异化优势

1. **高度拟人化封装**：不仅仅是AI接口，而是完整的"角色操作系统"
2. **极简部署**：`npm install && node src/index.js`，无需Docker、Python、数据库
3. **游戏化数值系统**：好感度/依赖度驱动角色行为，增强沉浸感
4. **本地优先架构**：完全离线可用（配合Ollama/llama.cpp）
5. **MCP协议原生支持**：AI可直接操作文件、浏览器、系统
6. **模块化设计**：19个文件清晰分层，易于维护和扩展

---

## 17. 总结与展望

### 17.1 当前架构优势

- **功能密度极高**：2631行代码实现了对话、工具、多模态、WebSocket、API、Web UI、记忆、意识等完整系统
- **模块化设计**：从单文件重构为19个模块，各模块职责清晰、可独立维护
- **多后端解耦**：三种LLM后端的统一抽象，可无缝切换
- **深度思考流**：实时`<thinking>`解析实现了"内心独白+回复"的分流展示
- **MCP标准**：采用Anthropic官方协议，13工具 + 资源 + 提示词 + 动态注册
- **Web管理界面**：完整的Web UI，无需额外启动前端服务
- **多对话系统**：隔离的对话存储，支持新建/切换/搜索/删除

### 16.2 已知局限

| 方面 | 现状 | 改进方向 |
|------|------|---------|
| 测试 | 零测试覆盖 | 添加单元测试和集成测试 |
| 错误恢复 | 有限 | 加入更完善的重试机制、断路器等 |
| Token管理 | 简估算法 | 接入tiktoken精确计数 |
| 扩展性 | 手动代码编辑 | 插件系统、动态工具注册增强 |
| 安全 | 无隔离 | 沙箱执行、路径白名单 |
| 持久化 | JSON文件 | 可选SQLite/PostgreSQL |
| Web前端 | 原生JS | 可选React/Vue重构，状态管理优化 |

### 16.3 未来展望

1. **多角色支持**：动态切换角色人设，支持用户自定义角色创建
2. **长期记忆**：引入向量数据库（如chroma/lancedb）进行语义检索
3. **语音交互**：集成TTS（如Edge-TTS/Fish Speech）和STT（Whisper）
4. **Web管理界面增强**：可视化配置、角色编辑、对话导出
5. **函数调用增强**：从MCP工具扩展到完整Function Calling协议
6. **多模态扩展**：支持视频帧分析、音频理解
7. **智能体框架**：支持多Agent协作、子Agent编排、任务规划
8. **移动端适配**：PWA或原生移动应用

---

## 附录

### A. 核心文件路径索引

| 组件 | 文件 | 行数 |
|------|------|------|
| 入口启动 | `src/index.js` | 138 |
| 配置系统 | `src/config.js` | 79 |
| 状态管理 | `src/state.js` | 74 |
| 核心聊天引擎 | `src/core/chat.js` | 169 |
| 提示词构建 | `src/core/prompt.js` | 122 |
| 潜意识循环 | `src/core/consciousness.js` | 87 |
| 记忆进化与复盘 | `src/core/evolve.js` | 106 |
| LLM流式抽象 | `src/llm/stream.js` | 276 |
| MCP协议服务 | `src/mcp/server.js` | 449 |
| OpenAI兼容API | `src/api/server.js` | 159 |
| WebSocket服务 | `src/ws/server.js` | 67 |
| 多对话管理 | `src/ws/conversations.js` | 94 |
| CLI聊天界面 | `src/cli/chat.js` | 381 |
| 配置向导 | `src/cli/wizard.js` | 107 |
| 文件选择器 | `src/cli/picker.js` | 92 |
| 终端显示效果 | `src/ui/display.js` | 102 |
| 剪贴板工具 | `src/utils/clipboard.js` | 61 |
| 图像处理 | `src/utils/image.js` | 20 |
| 在线更新 | `src/utils/update.js` | 170 |
| 动态模型列表 | `src/utils/models.js` | 105 |
| 端口管理 | `src/utils/port.js` | 48 |
| 更新服务器 | `scripts/update-server.js` | 90 |
| 更新包仓库 | `packages/manifest.json` | 10 |
| Web UI HTML | `web/index.html` | 111 |
| Web UI 样式 | `web/style.css` | 520 |
| Web UI 逻辑 | `web/app.js` | 403 |

### B. AI.PROMPT模板变量

| 变量 | 说明 | 来源 |
|------|------|------|
| `{{NAME}}` | 角色名字 | ROLE_INFO.name |
| `{{PERSONALITY}}` | 角色性格描述 | ROLE_INFO.personality |
| `{{TIME}}` | 当前时间 | `new Date().toLocaleString()` |
| `{{TIME_STATUS}}` | 时段（深夜/凌晨/上午/下午/傍晚） | 基于小时判定 |
| `{{HP}}` | 当前生命值 | state.HP |
| `{{MP}}` | 当前魔力值 | state.MP |
| `{{DESIRE}}` | 当前依赖度 | state.Desire |
| `{{LIKE}}` | 当前好感度 | state.Like |
| `{{RULES}}` | 角色规则（最多5条） | profile.rules |
| `{{FACTS}}` | 已知事实（最多5条） | profile.facts |
| `{{TOOLS}}` | MCP工具列表 | getToolDescriptions() |
| `{{STRATEGY}}` | 推理策略指令 | STRATEGY_DESC |

### C. 快速部署命令

```bash
# 安装依赖
npm install

# 首次运行（自动进入配置向导）
npm start

# 直接进入聊天
npm run chat

# 或
node src/index.js --chat
```

### D. 许可证

LGPL-2.1（GNU Lesser General Public License v2.1）
允许自由使用、修改、商用，修改后的衍生作品需以LGPL发布。

---

*本白皮书由AI辅助生成，技术细节基于 src/ 源代码 v3.1 分析。*
